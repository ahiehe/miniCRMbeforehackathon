mutation registerUser($user: UserRegisterInput!) {
    registerUser(
        user: $user
    ) {
        token
    }
}

mutation loginWithGoogleUser($code: String!){
    loginWithGoogleUser(code: $code){
        token
    }
}